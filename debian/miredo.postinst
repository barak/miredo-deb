#!/bin/sh

set -e

case "$1" in
  configure)
    if ! getent passwd miredo >/dev/null; then
      adduser --quiet --system --home /var/run/miredo --no-create-home miredo
    fi
  ;;
esac

#DEBHELPER#

